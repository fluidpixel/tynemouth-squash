<%= @greeting %>,

<% if @bookings %>
		<% @bookings.each do |booking| %>
			<%= booking.start_time.strftime("%l:%M%P") %> | <%= booking.court.court_name %> | <%= booking.player.first_name %> <%= booking.player.last_name %><% if booking.vs_player_name %> vs <%= booking.vs_player_name %><% end %>\r\n
	<% end %>
<% end %>

Print them for reference or 'just in case'

<h1>Membership Types</h1>
<!-- <div id="center" class="players"> -->

<% if is_super_admin %>
	<h2 class="newPlayer"><%= link_to 'New Membership', new_membership_type_path %></h2>
    <br />
<% end %>
<table class="booking">
  <% @types.each do |type| %>
  	<% if type.membership_type %>
    <tr>
      <td class="first wide"><%= link_to type.membership_type.humanize.titleize, membership_type_path(type.id) %></td><td class="cost"><%= number_to_currency(type.court_cost, :unit => "£") %>
    </tr>
	<% end %>
  <% end %>
</table>
<!-- </div> -->